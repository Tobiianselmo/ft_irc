-Manejar las señales.

-En el invite creo que cuando lo agrega vamos a tener que enviarle una respuesta al cliente. Tampoco quise tocarlo.

-Cree la funcion remClientFromServ(Client &client, int i); que busca el cliente y lo elimina de todos los canales, del mapa y del vector de pollfds.
Se podria pulir MUCHO mas. Pero para ahora va bien.

-Comprobar bien las respuestas en el INVITE, agregue varias pero no se si estan todas

-Mensaje para dar operador solo se entera el que realiza el comando.

- Tal vez crear una manera de mandar mensajes a canales pero unicamente a quien lo creo

-Si falla un mode por ejemplo /MODE +oi user(no existe el usuario), la funcion retorna y no sigue con el +i, se podria crear una respuesta del error
y no agregarlo al prefix, entonces podria continuar sin retornar 1 y cortar el modo.

-El ERR_KEYSET es igual que el ERR_INVALIDKEY?

-Si falla el addChannelKey ya dentro envia la respuesta de error, y fuera continua sin agregar el +k y sigue con el resto de comandos.